# Python Strings – Practice Set

This practice set is based on the topics we have covered so far:

- Strings in Python
- String Slicing and Indexing
- String Methods and Functions
- String Formatting and f-Strings
- String Manipulation
# 1. Basic String Operations

#### 1. Create a string variable `name` with your full name.

Print:

- The first character of the string
- The last character of the string
- The length of the string


#### 2. Concatenate the following two strings:

```python
"Hello"
"World"
```

Add a space between them and print the result.


# 2. String Slicing and Indexing

#### 1. Given the following string:

```python
text = "Python Programming"
```

Perform the following operations:

- Print the first 6 characters of the string.

- Print the last 6 characters of the string.

- Print every second character from the string.


#### 2. Reverse the string using slicing and print the result.


# 3. String Methods and Functions

#### 1. Perform the following operations on the given strings.


- Remove the extra spaces from both ends of the string and print the result.

- Convert the string to **Title Case** and print the result.

- Count how many times the letter `"o"` appears in the string.

Print the count.



#### 2. Check whether the following string is alphanumeric:

```python
text = "123abc"
```

Print the result.

# 4. String Formatting and f-Strings
### 1. Using `format()`, create a sentence:
`"My name is sarma and I am 20 years old."`

by passing "sarma" and 20 as variables.

### 2. Do the same using f-strings.

# 5. String Manipulation Challenges
#### 1. Given sentence = `"Coding in Python is fun"`, replace `"fun"` with `"awesome"` and print it.
#### 2. Find the index of the word `"Python"` in `sentence`.
#### 3. Convert the entire `sentence` to uppercase and print it.

# 6. Bonus Questions
#### 1. Write a program that counts how many vowels are in a given string.
#### 2. Take a user input string and check if it is a palindrome (same forwards and backwards).